import { PropertyEditorInfo } from "maishu-jueying";
import * as React from "react";
import { Props as ComponentProps } from "../components/carousel/index";
import { Form, Input, Button } from "antd";


export default function (propEditors: PropertyEditorInfo[]): JSX.Element {
    // let pcImagesEditor = propEditors.filter(o => o.prop == PC_IMAGE_URLS)[0];
    // console.assert(pcImagesEditor != null);

    return <Form layout="vertical">

    </Form>
}